vignette = true

# XorDev base + Complementary Reimagined r5.9-style SSAO.
# The AO uses Complementary's temporal dither and a small per-frame history
# buffer so the low-sample pattern resolves instead of producing repeated
# offset shadows or visible grid noise.
